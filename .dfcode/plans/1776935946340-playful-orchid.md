# solarDraw.vue 修改计划

## Context
1. 已完成的修改：right-panel 固定 1000x750，画布固定 1000x750，图片 object-fit: fill 铺满画布，坐标以画布左上角为原点。
2. 新增需求：修改面包屑颜色——第一个"光伏区域管理"设为蓝色，第二个"新增光伏区"设为白色。

## Approach
- 面包屑颜色修改：使用行内 style 直接修改，第一个改为 `color: blue`，第二个改为 `color: white`。
- 保持其他已有修改不变。

## Implementation Steps
1. 修改第一个 `el-breadcrumb-item` 的 style：`color: aquamarine` → `color: blue`
2. 修改第二个 `el-breadcrumb-item` 的 style：添加 `color: white`（或替换默认颜色）

## Verification
- 页面显示时，面包屑第一个项为蓝色，第二个项为白色

### Critical Files for Implementation
- `C:\Users\23658\Desktop\项目\cloud_api\cloud_api_all\cloud_api_web\src\components\variableMgt\solarMgt\solarPanelMgt\solarDraw.vue` — 修改面包屑颜色
